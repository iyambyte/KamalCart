spring.application.name=KamalCart

# Database Configuration (use .env overrides if present)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/kamalcart}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:K@malesh2508}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
management.security.enabled=false
spring.web.resources.static-locations=file:uploads/
